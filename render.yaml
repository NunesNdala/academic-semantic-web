services:
  - type: web
    name: academic-semantic-web
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd app && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
